using UnityEngine;

public class HighlighterTarget : MonoBehaviour
{
    [Tooltip("The manual Width (X) and Height (Y) the highlighter should be for this button.")]
    public Vector2 highlightSize = new Vector2(200, 60);
}